export default function AdminPlaceholder({ title }) {
  return (
    <div>
      <h3 className="fw-bold mb-2">{title}</h3>
      <div className="text-muted" style={{ fontSize: 13 }}>
        Página preparada para futura implementación.
      </div>
    </div>
  );
}

